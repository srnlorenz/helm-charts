apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "outline.fullname" . }}-init-script
  labels:
    app: {{ include "outline.name" . }}
    chart: {{ include "outline.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
data:
  {{ (.Files.Glob "files/healthcheck.js").AsConfig | nindent 2 }}
